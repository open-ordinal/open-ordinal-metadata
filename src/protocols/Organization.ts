import { Linked } from "../Links";
import { Collection } from "./Collection";

/**
 * @category Protocols
 */
export interface Organization {
    name: string;
    description?: string;
    url: string;
}
